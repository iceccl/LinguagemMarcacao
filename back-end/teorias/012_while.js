let contador = 0;

//  (NOVIDADE) Entra no bloco de comando pelo menos 1 vez
do {
    console.log(contador);
    contador++;
} while (contador < 5) {
    // Verifica a condição, se for true colta no início
    console.log('FIM');
    
}