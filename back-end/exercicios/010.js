function calculoArea(base, altura) {
    let calculo = (base * altura) / 2;
    console.log(`A area do triângulo correspondo à ${calculo}`);
}

calculoArea(20, 10);
calculoArea(10, 15);
calculoArea(100, 90);